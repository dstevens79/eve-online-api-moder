// Simple test to verify admin login functionality
// Testing credentials: admin / 12345

  password: '12345'

console.log('Userna


  const password = credentials.password?.trim() 
  console.log('Trimmed credentials:', { username, p
  // Check for admin login if configured

      characterId: 999999999,
      corporationId: 1000000000,
      isAdmin: true,
    };

  if (username === 'admin' && password === '12345') {
  
      characterName: 'Local Administrato
      corporationName: 'LMeve Administration',
      success: true
  }
  console.log('❌ Login failed
}
try {
  console.log('Login test result:', result);
  console.error('Log


























